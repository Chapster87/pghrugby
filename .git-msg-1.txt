Remove Medusa and Strapi; flatten app to repo root

Medusa fully removed from the frontend: store/collections/categories/
account/product routes, the src/modules tree, lib/data, and SDK deps
(548 packages dropped). Strapi deleted (strapi/, migrations/strapi/).
nextjs/ flattened to the repo root (migrations/ now holds dato-cms +
import-wp), package renamed pghrugby, check-env-variables.js rewritten
for the real runtime set, and env/README/docs updated. Fixes two
pre-existing build breaks: womens-standings import/props and the
Sanity studio root layout. Build still blocked by a flaky Next.js
[slug] prerender bug and the unreachable local ForgeCMS (see #16).
